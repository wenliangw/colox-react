/**
 * The eye outline — shared verbatim by Eye and EyeOff (paired-sibling
 * spec: the off state derives from the on state by substitution, not by
 * a second drawing). Lens rx8 ry7 centered on the canvas.
 */
export const eyeOutlinePath = 'M4 12 A8 7 0 0 1 20 12 A8 7 0 0 1 4 12';

/** The 45° cover slash of EyeOff. */
export const eyeSlashPath = 'M5 19 L19 5';
