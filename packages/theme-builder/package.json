{
  "name": "@colox/theme-builder",
  "version": "0.0.0",
  "description": "Compile the Colox design language: turn token sources (Figma exports; shipped builtin language included) into theme CSS plus runtime token artifacts, and a colox.theme.json into brand/custom theme CSS",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wenliangw/colox-react.git",
    "directory": "packages/theme-builder"
  },
  "bin": {
    "colox": "./cli/colox.mjs"
  },
  "files": [
    "cli",
    "dist",
    "schemas",
    "config",
    "src/styles",
    "scripts",
    "sd.config.mjs",
    "sd-dark.config.mjs",
    "sd-palette.config.mjs",
    "colox.theme.build.json"
  ],
  "exports": {
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "node cli/colox.mjs theme build && node scripts/emit-cli-data.mjs",
    "pretest": "pnpm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "style-dictionary": "^4.4.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
