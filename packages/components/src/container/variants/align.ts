/**
 * Horizontal placement of the constrained box. Centered is the shell
 * semantic (the container definition); `start` renders the block's own
 * default margins as an explicit opt-out.
 */
export const containerAlignStyles = {
  center: 'colox-container--align-center',
  start: 'colox-container--align-start',
} as const;
