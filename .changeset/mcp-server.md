---
'@colox/mcp': minor
---

Ship the official local MCP server: four read-only tools
(`search_doctrine`, `get_rule`, `get_skill`, `get_component`) over the
`@colox/wiki` doctrine data, registered with one `npx -y @colox/mcp`
line. Releases alongside `@colox/wiki`.