---
'@colox/react': minor
'@colox/wiki': minor
---

Add Slider: a single-thumb numeric range on a native `input type="range"`
(keyboard, focus and the form value untouched) with a palette-colored
traveled stripe (the six Button families, brand by default), tick marks
with labels below the strip, four size tiers on the shared row-height
scale, and a `{ event, value }` change payload with the committed number
parsed; per-subpath entry `@colox/react/slider`, preview stories and a
docs page; wiki component map updated in lockstep.