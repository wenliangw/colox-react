---
sidebar_position: 2
---

import { Button } from '@colox/react';
import '@colox/react/style.css';

# Button

Buttons trigger user actions.

## Variants

<Button variant="solid">Solid</Button> <Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>

## Intents

<Button intent="primary">Primary</Button> <Button intent="neutral">Neutral</Button>
<Button intent="danger">Danger</Button> <Button intent="warning">Warning</Button>
<Button intent="success">Success</Button>

## Sizes

<Button size="xs">Tiny</Button> <Button size="sm">Small</Button> <Button size="md">Medium</Button>
<Button size="lg">Large</Button>

## Props

| Prop    | Type                                                           | Default     |
| ------- | -------------------------------------------------------------- | ----------- |
| variant | `'solid' \| 'outline' \| 'ghost'`                              | `'solid'`   |
| intent  | `'primary' \| 'neutral' \| 'danger' \| 'warning' \| 'success'` | `'primary'` |
| size    | `'xs' \| 'sm' \| 'md' \| 'lg'`                                 | `'md'`      |

It also accepts all native `<button>` attributes; `type` defaults to `'button'`.
